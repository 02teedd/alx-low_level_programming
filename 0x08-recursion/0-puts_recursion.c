#include  <stdio.h>

/**
 * main - function that prints a string followed by new line.
 *
 * return: 0 if successful.
 */
int main(void)

{
	char c;

	printf("Puts with recursion");
		scanf("%c", c);
		return (0);
}
